# Infrastructure

This directory contains the Docker Compose configurations for local development and deployment.

Currently, it only contains the `postgres` service definition in `docker-compose.yml`. As the project progresses, this file will grow to include the NestJS `backend` container, the Next.js `customer` storefront container, and the `management` dashboard container, as well as an Nginx reverse proxy.
